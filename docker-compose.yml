services:
  macops:
    build: .
    ports:
      - "7034:3000"
    environment:
      APP_BASE_URL: http://localhost:7034
      DATABASE_URL: file:/data/macops.sqlite
      SESSION_SECRET: change-this-before-any-shared-environment
      AUTH_MOCK_ENABLED: "true"
      INTEGRATION_MODE: mock
    volumes:
      - macops-data:/data

volumes:
  macops-data:
